# There are also got assignment operators in Python that is for assigning values to varibles,
# these are the assignment operators in Python:
assignment_operators = {
    "=": "Assigns a value to a varibles",
    "+=": "Adds a value to a varibles and assigns the result to the varibles",
    "-=": "Subtracts a value from a varibles and assigns the result to the varibles",
    "*=": "Multiplies a vaarible by a value and assigns the result to the varibles",
    "/=": "Divides a vaarible by a value and assigns the result to the varibles",
    "%=": "Calculates the modulus of a vaarible by a value and assigns the result to the varibles",
    "//=": "Calculates the floor division of a variable by a value and assigns the result to the varibles",
    "**=": "Calculates the exponentiation of a variable by a value and assigns the result to the varibles"
}
# tips: you need two varibles to use all of the assignment operators except for the "="
# operators, for example:
# x = 10
# y = 5
# x += y
# x -= y
# x *= y
# x /= y
# x %= y
# x //= y
# x **= y