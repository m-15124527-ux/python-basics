# there are strings in python, they are used to store text data.
# for exmaple:
strings_operators = {
    "+" : "concatenation operator. It is used to combine two variables of type string.",
    "*" : "repetition operator. It is used to repeat a string a specified number of times."
}
# for example:
string1 = "Hello"
string2 = "World"
print(string1 +  string2) #if you want space, you can do it like this:
#print(string1 + " " + string2)you also can use the common way is at "" between them add space.
# repetition operator:
print(string1 * 3) #this will repeat the string three times
# there are also useful string methods in python, for example:
string_methods = {
    "len()" : "counts the characters in a string.",
    "upper()" : "converts a string to uppercase.",
    "lower()" : "converts a string to lowercase.",
    "in" : "checks letters are in a string or not."
}
string_comperison = {
    "==" : "Same text,same case.",
    "!=" : "Different text or different case."
}